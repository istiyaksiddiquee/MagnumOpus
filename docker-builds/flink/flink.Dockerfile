FROM flink:1.20-java11
# Nothing else yet — this stays minimal until the cluster is confirmed healthy.
# Fluss connector + Iceberg/Hadoop/Hive JARs get added here in the next step
# (to enable the Fluss->Iceberg tiering job).
