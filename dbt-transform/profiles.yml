bronze:
  target: dev
  outputs:
    dev:
      type: trino
      user: admin
      host: trino
      port: 8080
      database: iceberg
      schema: bronze
      threads: 1

silver:
  target: dev
  outputs:
    dev:
      type: trino
      user: admin
      host: trino
      port: 8080
      database: iceberg
      schema: silver
      threads: 1

gold:
  target: dev
  outputs:
    dev:
      type: trino
      user: admin
      host: trino
      port: 8080 
      database: iceberg
      schema: gold
      threads: 1
